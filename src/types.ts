export interface Canteen {
  id: number;
  name: string;
  hours: string;
  distance: string;
  density: string;
  statusClass: string;
  open: boolean;
  locationDesc?: string;
  capacity?: string;
  image?: string;
}
